<div id="results">
    <div class="row">
        <div class="large-12 columns">
            <h2>Your Results</h2>
        </div>
    </div>
    <div class="row">
        <% if !@session.has_viewed_results && @session.progress == 100.0 %>
            <div class="large-7 columns end">
                <!--<div class="panel callout">-->
                <div id="results-modal" class="reveal-modal medium" data-reveal>
                    <p>
                    <% if @lower_risk_count > @higher_risk_count %>
                        Congratulations! You have <%= @lower_risk_count %> lower risk indicators and only <%= @higher_risk_count %> higher risk ones. To learn more
                        about keeping your risk low, check out the information under <a href="/resources/more_info">Resources</a>.
                    <% else %>
                        You have some room for improvement&mdash;you have <%= @higher_risk_count %> higher risk indicators and <%= @lower_risk_count %> lower risk indicators. To learn more
                        about keeping your risk low, check out the information under <a href="/resources/more_info">Resources</a>.
                    <% end %>
                    </p>
                    <!--<a href="#" class="dismiss">Don't show this again</a>-->
                    <a href="/results/dismiss" class="close-reveal-modal">&#215;</a>
                </div>
            </div>
        <% end %>
    </div>
    <div class="row">
        <div class="large-7 columns end">
            <p>
                Each circle below represents your answer to a question. Click or tap a circle to return to that question.
            </p>
        </div>
    </div>
    <div class="row">
        <div class="small-12 medium-6 large-6 columns risk-infographic">
            <% @results.each do |category_id, result| %>
                <div class="risk <%= result["category_id"] %>">
                    <h3 class="risk-name"><%= result["category_name"] %></h3>
                    <div class="risk-level">
                        <% result["answers"].each do |answer| %>
                            <a href="/questionnaire/<%= answer["group_id"] %>" class="risk-question">
                                <img src="/images/icons/<%= result["category_id"] %>-<%= answer["risk"] %>.png"
                                    alt="<%= answer["question"] %>" />
                            </a>
                        <% end %>
                    </div>
                </div>
            <% end %>
        </div>
        <div class="inset small-12 medium-6 large-6 columns">
            <ul id="key">
                <li><img src="/images/high-risk-key.png" alt="" /> Higher Risk</li>
                <li><img src="/images/low-risk-key.png" alt="" /> Lower Risk</li>
                <li><img src="/images/unanswered-key.png" alt="" /> Unanswered Question</li>
            </ul>        
            <a href="/questionnaire" class="button small">Back to Questionnaire</a>
            <%= erb :share_buttons_partial %>
        </div>
    </div>
</div>